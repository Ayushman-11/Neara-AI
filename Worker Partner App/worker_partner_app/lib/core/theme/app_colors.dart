import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Surfaces ──────────────────────────────────────────────
  static const Color midnightNavy = Color(0xFF0D1117);
  static const Color warmCharcoal = Color(0xFF161B22);
  static const Color elevatedGraphite = Color(0xFF21262D);
  static const Color mutedSteel = Color(0xFF30363D);

  // ── Brand Accents ─────────────────────────────────────────
  static const Color saffronAmber = Color(0xFFF6A623);
  static const Color paleSaffron = Color(0xFFFBC95A);
  static const Color burntUmber = Color(0xFFC47D10);
  static const Color saffronGlow = Color(0x40F6A623);

  // ── Semantic / Status ────────────────────────────────────
  static const Color emergencyCrimson = Color(0xFFFF3B30);
  static const Color crimsonGlow = Color(0x40FF3B30);
  static const Color liveTeal = Color(0xFF00D4AA);
  static const Color safeGreen = Color(0xFF34C759);
  static const Color warningAmber = Color(0xFFFF9F0A);

  // ── Text ──────────────────────────────────────────────────
  static const Color brightIvory = Color(0xFFF0F6FC);
  static const Color softMoonlight = Color(0xFFB0BEC5);
  static const Color mutedFog = Color(0xFF6E7681);

  // ── Misc ──────────────────────────────────────────────────
  static const Color transparent = Colors.transparent;
}
